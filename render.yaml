services:
  - type: web
    name: validador-e-emissor-de-ingressos-qr-code
    env: node
    buildCommand: bun install && bun run build
    startCommand: node dist/server.cjs
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: APP_URL
        sync: false
